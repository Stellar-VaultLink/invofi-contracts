[package]
name = "invofi-common"
version = "0.5.0"
edition = "2021"
description = "Shared types and currency registry for InvoFi Soroban contracts"
license = "MIT"

[dependencies]
soroban-sdk = { workspace = true }

[features]
testutils = ["soroban-sdk/testutils"]
