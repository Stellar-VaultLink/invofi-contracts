[toolchain]
# soroban-sdk 22 is tested against 1.79.0. Pinning avoids post-MVP WASM
# feature regressions introduced in Rust 1.80+ for wasm32-unknown-unknown.
channel = "1.79.0"
targets = ["wasm32-unknown-unknown"]
